import { notFound } from "next/navigation";

/* /astra/p2 route removed from the live site. Originally this page rendered
   Astra Problem 2 (Approval Workflow Configuration). The original
   implementation is preserved in git history and can be restored by
   reverting this file. */
export default function AstraP2() {
  notFound();
}
