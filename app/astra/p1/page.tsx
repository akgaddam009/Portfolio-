import { notFound } from "next/navigation";

/* /astra/p1 route removed from the live site. Originally this page rendered
   Astra Problem 1 (AI Contract Review demo). The original implementation
   is preserved in git history and can be restored by reverting this file. */
export default function AstraP1() {
  notFound();
}
