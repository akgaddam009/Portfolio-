import { notFound } from "next/navigation";

/* /ai route removed from the live site. Originally this page rendered an
   index of AI exploration work. The original implementation is preserved
   in git history and can be restored by reverting this file. */
export default function AiPage() {
  notFound();
}
